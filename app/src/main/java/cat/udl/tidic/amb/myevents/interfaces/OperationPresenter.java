package cat.udl.tidic.amb.myevents.interfaces;

public interface OperationPresenter {
    void showResult (String bio);
    void invalidOperation();
    void onClick(String fullname, String description);
}
